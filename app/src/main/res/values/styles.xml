<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="TransitionTheme" parent="Theme.AppCompat.Light.NoActionBar"
        >
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowContentTransitions" tools:targetApi="lollipop">true</item>
    </style>
    
    <style name="TextAppearance.AppCompat.hint" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="MyEditText" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlActivated">@android:color/white</item>
    </style>

    <style name="NavTextStyle" parent="TextAppearance.AppCompat.Menu">
        <item name="android:textSize">30sp</item>
    </style>

    <style name="MyPopupWindow">
        <item name="android:windowEnterAnimation">@anim/popup_window_bottom_enter</item>
        <item name="android:windowExitAnimation">@anim/popup_window_bottom_exit</item>
    </style>


</resources>
